alertmanager:
  enabled: true
  config:
    global:
      resolve_timeout: 5m
      smtp_smarthost: "smtp.gmail.com:587"
      smtp_from: "alerts@gmail.com"
      smtp_auth_username: "engaimrathore@gmail.com"
      smtp_auth_password: "dnjt gpeq bfst qbiv"
      smtp_require_tls: true

    route:
      receiver: "email-alerts"
      group_by: ["alertname", "namespace"]
      group_wait: 30s
      group_interval: 5m
      repeat_interval: 4h

    receivers:
    - name: "email-alerts"
      email_configs:
      - to: "dixitrathi833@gmail.com"
        send_resolved: true

